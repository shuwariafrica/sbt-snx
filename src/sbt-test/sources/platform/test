# Per-platform source (scalanative-<os>) and resource (resources-<os>) directories are injected: the build only
# compiles + links because the resolved platform's Plat.scala and scala-native/plat.c are on the paths.
> run
