# The managed-dependency DSL (% NativeClassifier, forwarded config builders in either order, and the two-way
# conversion incl. the bare lift) compiles through the plugin autoImport, while plain `% Test` resolves through
# sbt's own path.
> check
