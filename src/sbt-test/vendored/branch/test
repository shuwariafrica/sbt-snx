# A vendored Git source may pin a branch: choosing a stable ref is the downstream user's responsibility, as it is
# for sbt's own git source dependencies. A branch is cloned once then cached and frozen per machine.
> setupRepo
> snxVendoredArtefacts
> check
