# A NIR library opts its test binary into a fully-static link via `Test / SNX.linkage := Static`, independent of the
# deliverable's dynamic linkage (the no-coupling default) - resolveTestTarget now honours it for every deliverable,
# where before a non-Executable test was forced dynamic. The test binary links statically and runs through the
# framework. Gated to static-capable platforms (musl/MSVC) by the scripted excludeFilter.
> checkStaticTest
> test
