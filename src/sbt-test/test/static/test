# A NIR library opts its test binary into a static C runtime via `Test / SNX.modifiers += SNX.staticRuntime`, scoped to
# the test binary and leaving the (unlinked) NIR deliverable untouched. checkStaticTest asserts the platform's sense of
# static (fully static on musl, a static /MT C runtime on MSVC); the binary then runs through the framework. Gated to
# the static-capable toolchains (musl/MSVC) by the scripted excludeFilter.
> checkStaticTest
> test
